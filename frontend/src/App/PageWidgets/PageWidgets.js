import React from 'react';

import { ToastContainer } from 'react-toastify';

const PageWidgets = function () {
    return (
        <React.Fragment>
            <ToastContainer />
        </React.Fragment>
    );
};

export { PageWidgets };
